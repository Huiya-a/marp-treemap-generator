# 技术文档

本文档包含布局算法、CSS 架构、配置参数和版本约束的详细说明。

---

## 布局算法

### 整体流程

1. **加载数据** — 从 Excel 读取 `{组名: [模块名]}` 字典
2. **计算每组 mpr** — `compute_modules_per_row(n)` 根据模块数 n 计算最优"每行模块数"，目标是列数/行数 ≈ 1.5
3. **确定列数** — `compute_optimal_column_count` 尝试 2~4 列，综合评分（模块分布平衡 + 视觉行数平衡 + 宽高比）选最优
4. **分配组到列** — `_assign_groups_to_columns` 按模块数降序贪心分配，平衡各列的模块数和视觉行数
5. **调整 mpr** — `_adjust_mpr_for_balance` 逐列尝试减小 mpr（3→2 等）来平衡列间高度
6. **行内打包** — `_pack_groups_into_rows` 列内组按宽度贪心打包成行
7. **等比缩放** — `scale = min(scale_x, scale_y)` 统一缩放适配画布
8. **生成 HTML** — 用 CSS flex 布局渲染列和组，输出 Marp Markdown

### 两种代码路径

| 条件 | 路径 | 特点 |
|------|------|------|
| 组数 < 6 | `compute_optimal_column_count` + `_assign_groups_to_columns` | 动态列数，平衡评分 |
| 组数 ≥ 6 | `_layout_many_groups` | 固定 mpr=3，贪心按行数填充列 |

### 列分配评分

```
score = module_dev + vr_dev²
```

- `module_dev` — 模块数偏差（当前列模块数 / 平均模块数）
- `vr_dev` — 视觉行数偏差（当前列行数 / 平均行数）
- 行数偏差用**平方惩罚**，优先平衡视觉高度而非仅平衡模块数

---

## 配置参数（src/config.py）

### 画布尺寸

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `CANVAS_W` / `CANVAS_H` | 13.33 / 7.5 | 自然单位画布尺寸 |
| `CANVAS_W_PX` / `CANVAS_H_PX` | 1280 / 720 | Marp 像素尺寸 |

### 模块尺寸

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `MODULE_W` / `MODULE_H` | 1.2 / 0.4 | 模块自然尺寸（3:1 宽高比） |
| `MODULE_GAP_X` / `MODULE_GAP_Y` | 0.05 / 0.065 | 模块间水平/垂直间距 |
| `MODULE_FONT_SIZE` | 14 | 模块文字字号 |
| `GROUP_HEADER_FONT_SIZE` | 18 | 组标题字号 |

### 间距

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `COL_GAP` | 0.2 | 列间间距（自然单位） |
| `ROW_GAP` | 0.12 | 组间垂直间距（自然单位） |

### 组内部

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `HEADER_H` | 0.3 | 标题栏高度 |
| `HEADER_GAP` | 0.15 | 标题栏与模块区域间距 |
| `GROUP_PAD_BOTTOM` | 0.2 | 组底部内边距 |
| `GROUP_PAD_X` | 0.35 | 组左右内边距 |

### 画布边距

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `OUTER_PAD_X` | 0.35 | 画布左右外边距 |
| `OUTER_PAD_TOP` | 0.75 | 画布顶部外边距 |
| `OUTER_PAD_BOTTOM` | 0.35 | 画布底部外边距 |

### 配色

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `GROUP_BG` | `#E0E0E0` | 组背景色 |
| `GROUP_BORDER` | `#BDBDBD` | 组边框色 |
| `GROUP_HEADER_COLOR` | `#1A73E8` | 组标题栏背景色 |
| `MODULE_BG_COLOR` | `#C4D8FC` | 模块背景色 |

### 布局控制

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `TARGET_RATIO` | 16/9 | 目标宽高比 |
| `ADJUST_MPR` | True | 是否启用 mpr 平衡调整 |

---

## CSS 架构

### 生成的 Markdown 结构

```
---
marp: true
style: |          ← CSS 样式块
  ...
---

<div class="treemap">
  <div class="domain-frame-wrapper">
    <div class="domain-frame">
      <div class="domain-title">域名称</div>
      <div class="columns">
        <div class="column">
          <div class="group">
            <div class="group-header">组名称</div>
            <div class="modules">
              <div class="mod-row" style="--mod-w:NNNpx">
                <div class="module">模块名</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

### 各选择器作用

| 选择器 | 层级 | 作用 |
|--------|------|------|
| `section` | — | Marp 默认容器，必须覆盖为 `display: block` |
| `.treemap` | 1 | 最外层容器，绝对定位铺满 slide |
| `.domain-frame-wrapper` | 2 | 域外框，提供背景色和边框 |
| `.domain-frame` | 3 | 域内容 flex 容器 |
| `.domain-title` | — | 域名称标题 |
| `.columns` | 4 | 列容器，横向排列 |
| `.column` | 5 | 单列，纵向排列各组 |
| `.group` | 6 | 应用组容器 |
| `.group-header` | — | 组名称标题栏（蓝色） |
| `.modules` | 7 | 模块网格容器 |
| `.mod-row` | 8 | 一行模块 |
| `.module` | 9 | 单个模块格子 |

### `--mod-w` CSS 变量机制

模块格子的固定宽度通过 CSS 变量实现：

```html
<div class="mod-row" style="--mod-w:107px; --mod-h:39px; --mod-h-inner:35px">
  <div class="module">模块名</div>
</div>
```

- 同一行的所有 `.module` 共享同一个 `--mod-w` 值，确保等宽
- `.module` 的 CSS 中 `width: var(--mod-w, 153px)` 引用该变量
- `flex: 0 0 auto` 确保格子不被拉伸
- `justify-content: center` 实现不满一行时居中

### `<br>` 换行机制

`_wrap_text` 函数对长模块名自动插入 `<br>`：

- 长度 ≤ 6：不换行
- 中文为主：在中文分隔符（括号、顿号、破折号等）处断行
- 英文为主：在单词边界处断行

---

## Marp CSS 注意事项

| 问题 | 解决方案 |
|------|----------|
| Marp 默认 `section` 是 flex-column | 必须用 `section { display: block !important; }` 覆盖 |
| `.treemap` 需要铺满 slide | 用 `position: absolute; top/left/right/bottom: 0` |
| CSS Grid 不可靠 | 用 flexbox 代替 |
| `.column` 的 flex 值 | 必须用 `flex: 1 1 0`，`flex: 0 0 auto` 在 Marp v4.4.0 不生效 |
| `!important` 部分被剥离 | Marp 会剥离部分 `!important`，不要完全依赖 |
| 文件名空格 | Marp CLI 不支持，输出时自动替换为下划线 |
| f-string 转义 | CSS 中使用双大括号 `{{` / `}}` |

---

## 版面约束（2026-06-02 确认）

> ⚠️ 版面已调试到接近期望形态，后续调整**不可破坏现有大体轮廓框架**。

### 核心结构（不可改动）

- 模块格子为**固定宽度矩形**，不满一行时**居中显示**，不拉伸填满
- 列分配使用 `score = module_dev + vr_dev²` 平衡视觉高度
- 组数 < 6 用动态列数，≥ 6 用固定 mpr=3 贪心填充
- Marp CSS 约束：section 必须 `display: block`，treemap 必须 `position: absolute`

### 可安全调整

- 颜色值、间距微调（≤20%）、字体大小、换行策略、目标比值（1.2~1.8）

### 禁止改动

- `--mod-w` CSS 变量机制、`justify-content: center` 居中策略
- section/treemap 定位方式、column 的 `flex: 1`
- 两套布局路径的切换逻辑（组数 6 为分界）

### 验证流程

```bash
python generate_treemap_md.py
marp output/*.md --images png --allow-local-files
# 逐张检查：模块等大居中、列高平衡、文字可读、无溢出
```
