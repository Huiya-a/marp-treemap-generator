# -*- coding: utf-8 -*-
"""
布局参数与配色配置

所有尺寸均为"自然单位"，布局引擎会计算统一的 scale 因子，
将自然单位映射到 Marp 幻灯片的像素尺寸 (1280×720)。
"""

# ============================================================
# 画布尺寸
# ============================================================
# 默认画布尺寸（自然单位），实际输出会根据内容自适应裁剪
CANVAS_W = 13.33   # 画布宽度
CANVAS_H = 7.5     # 画布高度

# Marp 幻灯片的像素尺寸（最终输出的分辨率）
CANVAS_W_PX = 1280  # 16:9 横向幻灯片
CANVAS_H_PX = 720

# Marp frontmatter 中的幻灯片高度（可调整，不影响模块缩放）
SLIDE_HEIGHT_PX = 720

# ============================================================
# 模块（Module）尺寸
# ============================================================
# 模块是 treemap 中最小的矩形单元，每个模块代表一个应用模块
MODULE_W = 1.2      # 模块宽度（自然单位）
MODULE_H = 0.4      # 模块高度（自然单位）— 宽高比 3:1
MODULE_GAP_X = 0.05 # 模块之间的水平间距（自然单位）
MODULE_GAP_Y = 0.065 # 模块之间的垂直间距（自然单位）
MODULES_PER_ROW = 3 # 默认每行模块数（会被动态计算覆盖）

# 字体大小（像素基准值，直接用于 CSS）
MODULE_FONT_SIZE = 14       # 模块文字字号
GROUP_HEADER_FONT_SIZE = 18 # 应用组标题字号

# ============================================================
# 组（Group）内部间距
# ============================================================
# 组 = 一组相关模块的集合，有蓝色标题栏
HEADER_H = 0.3         # 标题栏高度
HEADER_GAP = 0.15      # 标题栏与模块区域的间距
GROUP_PAD_BOTTOM = 0.2 # 组底部内边距
GROUP_PAD_X = 0.35     # 组左右内边距

# ============================================================
# 画布边距
# ============================================================
OUTER_PAD_X = 0.35    # 画布左右外边距
OUTER_PAD_TOP = 0.75  # 画布顶部外边距（留空较多，给标题）
OUTER_PAD_BOTTOM = 0.35  # 画布底部外边距

# ============================================================
# 域（Domain）边框
# ============================================================
# 域 = 最外层的矩形框，包含所有组，代表一个应用域
DOMAIN_PAD = 0.25   # 域内容的内边距
TITLE_H = 0.55      # 域标题的高度

# ============================================================
# 间距
# ============================================================
COL_GAP = 0.2  # 列与列之间的间距（自然单位，约 20px）
ROW_GAP = 0.12 # 行与行之间的间距（组之间，自然单位，约 12px）

# ============================================================
# 配色方案
# ============================================================
GROUP_BG = '#E0E0E0'           # 组背景色（浅灰）
GROUP_BORDER = '#BDBDBD'       # 组边框色
GROUP_HEADER_COLOR = '#1A73E8' # 组标题栏背景色（蓝色）
MODULE_BG_COLOR = '#C4D8FC'    # 模块背景色（浅蓝）

# 域（Domain）配色
DOMAIN_BG = '#F0F4F8'              # 域外框背景色
DOMAIN_BORDER_COLOR = '#2C3E50'    # 域外框边框色
DOMAIN_TITLE_COLOR = '#2C3E50'     # 域标题文字颜色

# 模块文字颜色
MODULE_TEXT_COLOR = '#1A1A1A'       # 模块内文字颜色
MODULE_BORDER_COLOR = 'white'      # 模块边框颜色

# ============================================================
# 布局控制
# ============================================================
TARGET_RATIO = 16 / 9  # 目标宽高比（横向 16:9）

# 布局开关
ADJUST_MPR = True  # 是否启用组内 mpr 调整（平衡列间高度）

# ============================================================
# 域外框样式
# ============================================================
DOMAIN_BORDER_WIDTH = 3       # 域外框边框宽度 (px)
DOMAIN_BORDER_RADIUS = 12     # 域外框圆角 (px)
DOMAIN_PADDING_X = 16         # 域外框水平内边距 (px)
DOMAIN_PADDING_Y = 12         # 域外框垂直内边距 (px)

# ============================================================
# 域标题样式
# ============================================================
DOMAIN_TITLE_FONT_SIZE = 22   # 域标题字号 (px)
DOMAIN_TITLE_MARGIN_BOTTOM = 8  # 域标题下方间距 (px)

# ============================================================
# 组样式
# ============================================================
GROUP_BORDER_RADIUS = 6       # 组圆角 (px)
GROUP_HEADER_MARGIN_BOTTOM = 4  # 组标题下方间距 (px)
COLUMN_GAP = 8                # 列内组间距 (px)

# ============================================================
# 模块样式
# ============================================================
MODULE_BORDER_RADIUS = 3      # 模块圆角 (px)
MODULE_LINE_HEIGHT = 1.3      # 模块行高倍数

# ============================================================
# 字体族
# ============================================================
FONT_FAMILY = '"Microsoft YaHei", sans-serif'
