# 应用架构图生成器 — 布局算法与生成流程详解

## 一、整体数据流

```
Excel (.xlsx)
    │
    ▼
data_loader.py          ── 解析 Excel，提取域、组、模块
    │
    ▼
layout.py               ── 核心布局算法，计算所有元素的自然坐标与缩放
    │
    ▼
generate_treemap_md.py  ── 两遍布局 + CSS 像素转换 + HTML 渲染 + Marp Markdown 输出
    │
    ▼
output/*.md             ── Marp 兼容的 Markdown 文件
    │
    ▼  (可选)
marp CLI                ── 转换为 PNG / PPTX
```

---

## 二、Excel 数据解析（data_loader.py）

### 输入格式

Excel 文件须包含一个名称含"应用模块清单"的 sheet（否则取第二个 sheet）。数据从第 3 行开始：

| 列 | 含义 |
|----|------|
| B 列 | 应用域名称（如"纪检监察域"） |
| D 列 | 应用组名称（如"信访管理"） |
| G 列 | 一级模块名称（如"来信登记"） |

### 解析逻辑

1. 遍历第 3 行起的每一行
2. 从 B 列提取域名称（取第一个非空值）
3. 用 `set()` 存储每个组下的模块（自动去重）
4. 最终对每个组的模块列表按字母排序
5. 返回 `(domain_name, {group_name: [sorted_modules]})`

---

## 三、自然单位体系（config.py）

所有布局计算使用"自然单位"（natural units），最终通过统一缩放因子映射到 1280×720 像素的 Marp 幻灯片。

### 画布与基础尺寸

| 参数 | 值 | 说明 |
|------|---|------|
| `CANVAS_W` | 13.33 | 画布宽度（自然单位） |
| `CANVAS_H` | 7.5 | 画布高度（自然单位） |
| `CANVAS_W_PX` | 1280 | 输出像素宽度 |
| `CANVAS_H_PX` | 720 | 输出像素高度 |

### 模块尺寸

| 参数 | 值 | 说明 |
|------|---|------|
| `MODULE_W` | 1.2 | 模块矩形宽度 |
| `MODULE_H` | 0.4 | 模块矩形高度（3:1 宽高比） |
| `MODULE_GAP_X` | 0.05 | 模块水平间距 |
| `MODULE_GAP_Y` | 0.065 | 模块行间距 |

### 组尺寸

| 参数 | 值 | 说明 |
|------|---|------|
| `HEADER_H` | 0.3 | 组标题栏高度 |
| `HEADER_GAP` | 0.15 | 标题与模块区的间距 |
| `GROUP_PAD_BOTTOM` | 0.2 | 组底部内边距 |
| `GROUP_PAD_X` | 0.35 | 组左右内边距 |

### 间距

| 参数 | 值 | 说明 |
|------|---|------|
| `COL_GAP` | 0.2 | 列间水平间距 |
| `ROW_GAP` | 0.12 | 组间垂直间距 |
| `OUTER_PAD_X` | 0.35 | 画布左右外边距 |
| `OUTER_PAD_TOP` | 0.75 | 画布顶部外边距（预留标题） |
| `OUTER_PAD_BOTTOM` | 0.35 | 画布底部外边距 |

### 目标参数

| 参数 | 值 | 说明 |
|------|---|------|
| `TARGET_RATIO` | 16/9 ≈ 1.778 | 目标宽高比 |
| `ADJUST_MPR` | True | 是否启用 mpr 平衡调整 |

---

## 四、布局算法详解（layout.py）

布局引擎根据组数选择两条不同的代码路径：

- **组数 < 6**：动态列数 + 平衡评分
- **组数 ≥ 6**：固定 mpr=3 + 贪心行填充

### 4.1 单个组的尺寸计算：`_raw_group_size(n_modules, mpr)`

一个组的自然尺寸由模块数和每行模块数（mpr）决定：

**宽度公式：**
```
width = mpr × MODULE_W + (mpr - 1) × MODULE_GAP_X + 2 × GROUP_PAD_X
```

示例（mpr=3）：
```
width = 3 × 1.2 + 2 × 0.05 + 2 × 0.35 = 4.4 自然单位
```

**高度公式：**
```
n_rows = ⌈n_modules / mpr⌉
height = HEADER_H + HEADER_GAP + n_rows × MODULE_H + max(0, n_rows-1) × MODULE_GAP_Y + GROUP_PAD_BOTTOM
```

示例（7 个模块，mpr=3 → 3 行）：
```
height = 0.3 + 0.15 + 3 × 0.4 + 2 × 0.065 + 0.2 = 1.98 自然单位
```

### 4.2 每行模块数选择：`compute_modules_per_row(n_modules)`

这个函数决定每个组中模块横向排列的数量（mpr），目标是让组的宽高比接近 **1.5**（略偏横向矩形）。

**算法：**
1. 若 `n_modules ≤ 3`：直接返回 `n_modules`（全部排一行）
2. 否则：遍历 `m = 2, 3, ..., min(n_modules, 6)`
   - 对每个候选 `m`，计算 `n_rows = ⌈n_modules / m⌉`
   - 计算宽高比 `ratio = m / n_rows`
   - 选择使 `|ratio - 1.5|` 最小的 `m`

**示例：**
- 7 个模块：候选 m=2(ratio=1.0), m=3(ratio=1.5), m=4(ratio=1.33) → 选 m=3（ratio 恰好 1.5）
- 10 个模块：候选 m=3(ratio=1.0), m=4(ratio=1.33), m=5(ratio=1.25) → 选 m=4（ratio 最接近 1.5）
- 5 个模块：候选 m=2(ratio=1.0), m=3(ratio=1.5), m=4(ratio=1.33) → 选 m=3（ratio=1.5）

### 4.3 辅助函数

**`_visual_rows(n_modules, mpr)`**：返回 `⌈n_modules / mpr⌉ + 1`（+1 算上标题行），用作列高度的快速估算。

**`_col_height(col_groups)`**：列的自然高度 = 所有组高度之和 + `(n_groups - 1) × ROW_GAP`。

### 4.4 组打包到行：`_pack_groups_into_rows(groups_in_col, col_width)`

将列内的多个组打包成水平"行"（一行可以包含多个并排的组）。这是一个**贪心算法**：

1. 将第一个组放入当前行
2. 对后续每个组，检查两个约束：
   - **行数约束**：候选组的模块行数 ≤ 当前行中已有组的模块行数（确保视觉一致性，矮组不排在高组前面）
   - **宽度约束**：当前行所有组的宽度 + 间距 + 候选组宽度 ≤ 列宽
3. 两个约束都满足 → 加入当前行（行高度取所有组的最大值）
4. 任一约束不满足 → 开始新行

返回 `(rows, row_heights)`，每个 row 是一组组的元组列表。

### 4.5 路径一：动态列数（组数 < 6）

#### 步骤 1：确定最优列数 `compute_optimal_column_count(group_info)`

遍历候选列数 2、3、4（上限为 `min(n_groups, 4)`），对每个候选列数：

1. 执行 `_assign_groups_to_columns` 获得实际分组
2. 计算三个评分维度：
   - **模块数不平衡度**：各列模块数与平均值的偏差平方和
   - **视觉行不平衡度**：各列视觉行数与平均值的偏差平方和
   - **宽高比偏差**：估算布局宽高与 `TARGET_RATIO` 的差异

3. **综合评分**：
   ```
   score = module_imbalance + vr_imbalance × avg_m + ratio_diff × avg_m × 2
   ```
   其中 `avg_m` 是平均模块数，用于缩放惩罚权重；宽高比惩罚权重为 2 倍。

选择综合评分最低的列数。

#### 步骤 2：分配组到列 `_assign_groups_to_columns(group_info, ncols)`

采用**最大组优先的贪心分配**：

1. 按模块数降序排列所有组（最大的先分配）
2. 对每个组，评估放入每一列后的评分：
   ```
   module_dev = 列当前模块数 / 平均模块数
   vr_dev = 列当前视觉行数 / 平均视觉行数
   score = module_dev + vr_dev²
   ```
   视觉行偏差使用**平方惩罚**，对列高度不平衡的惩罚远大于模块数不平衡。

3. 将组放入评分最低的列

#### 步骤 3：mpr 平衡调整 `_adjust_mpr_for_balance(col_groups)`

在组分配完成后，调整各列的 mpr 值以均衡列高度：

1. 计算每列当前高度
2. 以最高列为基准 `target_h = max(col_h)`
3. 对每个较矮的列，遍历其组：
   - 尝试降低 mpr（如从 3 降到 2），这会增加模块行数从而增加组高度
   - 检查：调整后高度是否增加？调整后列高度是否不超过 `target_h`？
   - 选择满足条件的最低 mpr

**关键约束**：只降低 mpr（增加行数 → 增加高度），不升高 mpr。矮列的组变高，使所有列趋于等高。

### 4.6 路径二：多组布局 `_layout_many_groups(data)`（组数 ≥ 6）

当组数较多时，使用完全不同的策略：

1. **固定 mpr = 3**，所有组统一
2. 预计算每个组的行数：`row_count = ⌈n_modules / 3⌉ + 1`（+1 为标题行）
3. **贪心装箱**：
   - 尝试目标行数从 10 递减到 5
   - 对每个目标行数，容差从 0 递增
   - 用 `_find_combination` 贪心分配：顺序遍历组，只要当前列的行数不超过 `target + tolerance` 就继续放入，否则开新列
   - 找到有效方案即停止

---

## 五、两遍布局与 CSS 像素转换（generate_treemap_md.py）

### 5.1 `_compute_structure(data)` — 核心桥梁

这个函数在布局引擎和 HTML 渲染之间架起桥梁，执行**两遍布局**：

#### 第一遍：探测内容边界

```
_run_layout(CANVAS_W, CANVAS_H)   # 用默认 13.33 × 7.5 画布
```

计算实际内容（列）的边界框：所有列占据的总宽度和最大高度。

#### 画布自适应

根据内容边界框 + 外边距 + 标题高度，计算紧凑的画布尺寸：
- 如果宽高比 < 16:9，等比扩展宽度以维持 `TARGET_RATIO`

#### 第二遍：最终布局

```
_run_layout(adjusted_w, adjusted_h)   # 用调整后的画布尺寸
```

获得最终的列分组和缩放因子。

### 5.2 CSS 像素转换

两遍布局完成后，将自然单位转换为 Marp 幻灯片的 CSS 像素：

1. 计算"自然"列宽（CSS 像素）：使用默认模块尺寸（153px 宽、51px 内高）
2. 设定 frame wrapper 填满可用宽度：`available_w = 1280 - 2×20 = 1240px`
3. 计算填充缩放因子：`fill_scale = frame_inner_w / natural_content_w_px`
4. 对所有 CSS 尺寸应用 `fill_scale`：
   - 模块宽度、高度
   - 行间距、模块间距
   - 组标题高度、组内边距
   - 字体大小

---

## 六、HTML 渲染

### 6.1 单个组的 HTML 结构

```html
<div class="group">
  <div class="group-header">组名称</div>
  <div class="modules">
    <div class="mod-row" style="--mod-w:136px; --mod-h:55px; --mod-h-inner:51px">
      <div class="module">模块1</div>
      <div class="module">模块2</div>
      <div class="module">模块3</div>
    </div>
    <div class="mod-row" style="--mod-w:136px; --mod-h:55px; --mod-h-inner:51px">
      <div class="module">模块4</div>
      <div class="module">模块5</div>
      <div class="module-empty"></div>  <!-- 不满一行时填充空格 -->
    </div>
  </div>
</div>
```

### 6.2 模块尺寸计算

每个 mod-row 通过 CSS 变量控制模块大小：

- **模块宽度** `--mod-w`：
  ```
  mod_w = (col_w - 2×11px padding - (mpr-1)×6px gap) / mpr
  ```

- **模块高度** `--mod-h`：
  ```
  mod_h = mod_w / 3 + 4px    // 保持 3:1 宽高比 + 边框/内边距
  ```

- 不满一行的模块数量较少，但宽度不变，通过 `justify-content: center` 居中显示（不拉伸填满整行）

### 6.3 文本换行：`_wrap_text(mod_name)`

CJK 感知的智能换行，插入 `<br>` 标签：

1. 若模块名 ≤ 6 个字符，不换行
2. 若中点字符为英文，在单词边界处断行（使两半尽量均衡）
3. 若中点字符为中文，向前搜索分隔符 `、-—/`，向后搜索 `）、-—/`（各在 3 字符范围内），取最近的断点

---

## 七、Marp Markdown 输出

### 7.1 文件结构

```markdown
---
marp: true
theme: default
paginate: false
style: |
  /* 大量 CSS 样式 */
---

<div class="section">
  <div class="treemap">
    <div class="domain-frame-wrapper">
      <div class="domain-frame">
        <div class="domain-title">纪检监察域</div>
        <div class="columns">
          <div class="column">
            <!-- 组的 HTML -->
          </div>
          <div class="column">
            <!-- 组的 HTML -->
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
```

### 7.2 关键 CSS 约束（Marp 特有）

| 选择器 | 关键样式 | 原因 |
|--------|---------|------|
| `section` | `display: block` | 覆盖 Marp 默认的 flex-column 布局 |
| `section` | `position: relative` | 作为 treemap 绝对定位的参考 |
| `.treemap` | `position: absolute; top/left/right/bottom: 0` | 强制填满整个 section |
| `.columns` | `display: flex` | 水平排列列 |
| `.column` | `flex: 1 1 0` | 列等宽分配 |
| `.module` | `flex: 0 0 auto` + CSS 变量 | 固定宽度，不拉伸 |
| `.mod-row` | `justify-content: center` | 不满一行时居中 |

### 7.3 列宽分配

列宽按各列首组的 mpr 比例分配（而非等宽），这样 mpr 大的列获得更多宽度，内部模块尺寸更一致。

---

## 八、输出与转换

### 命令行模式

```bash
# 生成所有 Excel 文件的 Markdown
python generate_treemap_md.py

# 处理单个文件（前缀或包含匹配）
python generate_treemap_md.py 03
python generate_treemap_md.py 纪检监察

# 转换为 PNG
marp output/*.md --images png --allow-local-files

# 转换为 PPTX
marp output/*.md --pptx
```

### GUI 模式

```bash
python src/app.py
```

GUI 通过 `GenerateWorker` 后台线程处理：
1. 加载 Excel → `load_data_from_excel`
2. 生成 Markdown → `generate_marp_md`
3. 可选调用 Marp CLI 转换为 PNG/PPTX/HTML
4. GUI 参数（颜色、尺寸、字体等）在生成时临时覆盖 config 模块的默认值，生成后恢复

### 等比宽度模式（`--proportional-width`）

处理多个 Excel 文件时，先计算每个文件的列数，然后按列数比例分配 frame 宽度，使不同幻灯片中的列宽具有可比性。

---

## 九、算法总结流程图

```
┌─────────────────────────────────────────────────────┐
│  Excel 解析                                          │
│  data_loader: 域 → {组: [模块]}                       │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  每个组确定 mpr（每行模块数）                           │
│  compute_modules_per_row: 目标 ratio = 1.5           │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  判断组数                                             │
│  ┌──────────────┐    ┌──────────────────────────┐   │
│  │  组数 < 6     │    │  组数 ≥ 6                  │   │
│  │              │    │                          │   │
│  │ 1.确定列数    │    │ 1.固定 mpr=3             │   │
│  │  2-4列,评分   │    │ 2.贪心装箱              │   │
│  │              │    │  目标行数 10→5           │   │
│  │ 2.分配组到列  │    │  逐步放宽容差            │   │
│  │  最大组优先    │    └──────────────────────────┘   │
│  │  评分=模块偏差 │                                   │
│  │    +行偏差²   │                                   │
│  │              │                                   │
│  │ 3.mpr平衡    │                                   │
│  │  矮列降mpr   │                                   │
│  └──────────────┘                                   │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  两遍布局（generate_treemap_md.py）                   │
│  第1遍: 默认画布 → 测量内容边界                        │
│  自适应画布 → 按 TARGET_RATIO 调整宽度                │
│  第2遍: 调整后画布 → 最终列分组与缩放                  │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  CSS 像素转换                                        │
│  fill_scale = 可用宽度 / 自然内容宽度                  │
│  所有尺寸 × fill_scale                               │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  HTML 渲染                                          │
│  组 → <div class="group">                           │
│  模块 → <div class="module"> + CSS 变量              │
│  文本 → _wrap_text 智能换行                          │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  Marp Markdown 输出                                  │
│  YAML frontmatter + 嵌入 CSS + HTML body            │
│  → output/<name>.md                                 │
└──────────────────────┬──────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────┐
│  （可选）Marp CLI 转换                               │
│  .md → .png / .pptx                                 │
└─────────────────────────────────────────────────────┘
```
